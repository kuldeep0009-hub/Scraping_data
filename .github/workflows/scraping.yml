name: Daily Domestic Flight Scraper

concurrency:
  group: daily-scrape
  cancel-in-progress: true

on:
  schedule:
    - cron: "30 2 * * *"
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
